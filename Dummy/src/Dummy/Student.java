package Dummy;

public class Student {
	private int id;
	private String name;
	
	public Student(int id, String name) {
		this.id = id;
		this.name = name;
	}
	
	public int getId() {
		return id;
	}
	
	public String getName() {
		return name;
	}
	/*
	 * ask information 
	 * - ask id
	 * - ask name
	 * store it in variable 
	 * using constructor 
	 * 
	 */
	
	/*
	 * return int 
	 * int id = method()
	 * string name = name()
	 * 
	 * new (id, name)
	 * 
	 * student array 3 + to string method  
	 */
}
